"""
config.py

Centralized configuration for the ride-video-agent application.

This module will be responsible for:
- Defining default paths (sample_data/, output/, etc.)
- Holding tunable parameters for scene detection, video export settings,
  and analysis thresholds
- Loading overrides from environment variables or a config file, if needed
- Exposing a single configuration object/dict for other modules to import

No logic implemented yet — scaffolding only.
"""
